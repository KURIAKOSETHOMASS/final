import React from 'react';
import Admin from './View'; // Assuming both files are in the same directory

const View = () => {
  return (
    <div>
      <h1>Admin Page</h1>
      <View />
    </div>
  );
};

export default View;
